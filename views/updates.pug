doctype html
html 
    head 
        title= `${title}`
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="stylesheet", href="/css/style.css")
        link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css")
    body    
        .container
            h1 Update custom object table
            .form
                form(method="POST")
                    label(for="newName") Character name
                    input(type="text" name="newName" required)
                    label.mt-3(for="newName") Character bio
                    input(type="text" name="newBio" required)
                    label.mt-3(for="newName") Character catchphrase
                    input(type="text" name="newCatchphrase" required)
                    input(type="submit" value="Submit")
            a.d-block.mt-5(href="/") Return to the homepage